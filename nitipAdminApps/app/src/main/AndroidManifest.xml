<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cindodcindy.nitipadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NitipAdmin">
        <activity android:name=".view.DetailConfirmBeforeDoneActivity"></activity>
        <activity android:name=".view.ConfirmPaymentFromBuyerActivity" />
        <activity android:name=".view.BottomNav" />
        <activity android:name=".view.SellerDoneDetailActivity" />
        <activity android:name=".view.BuyerPaymentDetailActivity" />
        <activity android:name=".view.AdminPaymentDetailActivity" />
        <activity android:name=".view.InputPaymentActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.RegisActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>